"""
evaluation.py — offline metrics + plots for the demo/presentation
Owner: Person D

balanced accuracy, F1/AUROC/PR-AUC per drug, Brier score + reliability plot,
no-call rate, performance by genetic cluster group. All functions should take
(y_true, y_pred, y_prob) as input.
"""

# TODO(Person D): metrics + matplotlib/plotly charts
